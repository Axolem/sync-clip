# Local secrets directory (gitignored contents except this file).
# Place keystores and App Store Connect API keys here; never commit them.
#
# Example:
#   secrets/sync-clip-upload.jks
#   secrets/AuthKey_XXXXXXXXXX.p8
